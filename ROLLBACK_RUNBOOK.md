# SoloForge 回滚手册

> 适用：`0.1.x` 内部版本；任何回滚都应由负责人执行并留存证据。

## 触发条件

出现数据损坏、迁移失败、应用无法启动、审批绕过、审计缺失、外发假成功、凭证泄露或升级后健康检查失败时，立即停止推广和高危操作。

## 步骤

1. 停止分发新制品，通知内部用户暂停高危动作。
2. 保留问题版本、安装包、commit、日志、审计导出和数据库备份；不得删除或修改 AuditLog。
3. 记录影响范围与 trace_id，隔离受影响 Workspace/Target。
4. 停止应用及相关远端升级任务。
5. 按 `docs/DATABASE_RELEASE_MIGRATION_PLAN.md` 的版本匹配方案恢复上一安装包；如迁移不可逆，先在副本恢复验证，禁止直接覆盖唯一数据库。
6. 恢复数据库前核对备份 hash、版本和文件权限；凭证从 Keychain 恢复或重新录入，不从日志复制。
7. 启动旧版本，验证数据可读、审批状态、审计 hash 链和连接诊断。
8. 对远端 OpenClaw/容器使用已记录的旧 image tag 或目标提供的 rollbackCommand；未配置回滚命令时不得臆造命令。
9. 记录恢复结果、失败原因和后续修复版本，负责人作出 No-Go/恢复推广决定。

## 验证清单

- [ ] 应用可启动且版本正确
- [ ] Workspace、工单和凭证引用可读
- [ ] AuditLog 未被修改/删除且 hash 可验证
- [ ] 未批准高危动作仍被阻止
- [ ] 外部连接失败不会破坏本地数据
- [ ] 备份、恢复和旧版本启动证据已归档

回滚不是删除失败记录，也不是通过修改文档掩盖失败。无法证明数据完整性时，保持 No-Go 并升级给项目负责人。
