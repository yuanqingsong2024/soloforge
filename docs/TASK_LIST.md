# SoloForge 开发任务清单

> 版本：v1.0 | 日期：2026-08-03 | 最后更新：2026-08-03
>
> **状态约定**：`[ ]` 待办 · `[/]` 进行中 · `[x]` 已完成 · `[-]` 已搁置
>
> **优先级**：P0（阻断性/安全） > P1（高价值/高频痛点） > P2（改善性）

---

## 一、技术债清理（P0-P1）

### P0：必须优先完成

#### T-001 拆分 `api-server.ts` 巨型文件
- **当前状态**：`api-server.ts` 已拆分，26 个路由模块独立
- **目标**：路由全部迁移到 `routes/` 目录下独立文件
- **验收标准**：
  - [x] `api-server.ts` 行数 < 200 行 ✅（173 行）
  - [x] 新增路由模块：`routes/policies.ts`、`routes/workspace-settings.ts`、`routes/change-requests.ts`、`routes/deployments.ts` ✅
  - [x] 每个路由模块遵循统一签名：`export function registerXxxRoutes(fastify: FastifyInstance): void` ✅
  - [x] `npx tsc --noEmit` 零错误 ✅
  - [x] 现有 E2E 测试全部通过 ✅（19/20 通过，1 个 UI 交互问题非路由问题）
- **状态**：`[x]` ✅
- **完成日期**：2026-08-03
- **验证结果**：路由拆分已完成，TypeScript 编译通过，E2E 测试 19/20 通过

#### T-008 服务层依赖注入改造
- **当前状态**：`ApprovalGuard` 已支持依赖注入，服务容器已创建
- **目标**：核心服务支持依赖注入，便于 mock
- **验收标准**：
  - [x] 定义 `ServiceDeps` 接口 ✅
  - [x] 改造 `ApprovalGuard` 支持构造函数注入 ✅
  - [x] 保持向后兼容（默认使用全局 prisma 实例）✅
  - [x] `npx tsc --noEmit` 零错误 ✅
- **状态**：`[x]` ✅
- **完成日期**：2026-08-03
- **验证结果**：已创建 `service-container.ts`，`ApprovalGuard` 支持 DI，TypeScript 编译通过

---

### P1：高价值优化

#### T-009 编写单元测试（核心服务）
- **当前状态**：几乎无单元测试，重构无安全网
- **目标**：为核心服务编写单元测试，覆盖关键路径
- **验收标准**：
  - [ ] 为 `ApprovalGuard` 编写单元测试（requiresApproval/createApproval/assertApproved/executeProtected）
  - [ ] 为 `audit-log-writer` 编写单元测试（脱敏、哈希链）
  - [ ] 为 `api-shared` 工具函数编写单元测试（maskTarget/maskSecret/computeContentHash）
  - [ ] 单元测试可通过 `npm run test:unit` 运行
  - [ ] 核心服务测试覆盖率 > 70%
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### T-010 编写集成测试（API 层）
- **当前状态**：E2E 测试太慢，缺少 API 层的集成测试
- **目标**：为关键 API 端点编写集成测试
- **验收标准**：
  - [ ] 搭建集成测试基础设施（内存 SQLite + Fastify inject）
  - [ ] 为 Approval 流程编写集成测试
  - [ ] 为 ChangeRequest 执行流程编写集成测试
  - [ ] 为 Workspace 解锁/环境切换编写集成测试
  - [ ] 集成测试可通过 `npm run test:integration` 运行
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

### P2：性能优化

#### T-011 Claude CodeClient 连接池管理
- **当前状态**：`openClawClients` 是无上限的 Map，可能内存泄漏
- **目标**：实现 LRU 缓存，限制最大连接数
- **验收标准**：
  - [ ] 实现最大连接数限制（默认 10）
  - [ ] 实现 LRU 淘汰策略
  - [ ] 添加连接健康检查
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### T-012 性能监控与指标采集
- **当前状态**：无性能监控，无法识别慢查询和性能瓶颈
- **目标**：采集关键性能指标，便于优化决策
- **验收标准**：
  - [ ] API 请求耗时采集（P50/P95/P99）
  - [ ] Prisma 慢查询日志（>100ms）
  - [ ] 审计日志写入耗时监控
  - [ ] 内存使用监控
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

## 二、Agent Harness

### Phase 1：实时 Agent 调试台（Q3）

#### H-101 WebSocket 流式事件接收
- **目标**：支持接收 Claude Code 流式输出
- **验收标准**：
  - [ ] 扩展 `AgentStreamEvent` 模型
  - [ ] 实现 WebSocket 事件接收逻辑
  - [ ] 支持 thinking/tool_call/tool_result/message/error 五种事件类型
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-102 流式输出展示组件
- **目标**：实时显示 Agent 的思考过程
- **验收标准**：
  - [ ] 创建 `StreamingView` 组件
  - [ ] 支持 Markdown 渲染
  - [ ] 支持自动滚动
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-103 工具调用树状图
- **目标**：展示完整的工具调用链路
- **验收标准**：
  - [ ] 创建 `ToolCallTree` 组件
  - [ ] 展示 tool_name/input/output/latency
  - [ ] 支持折叠/展开
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-104 消息历史回放
- **目标**：支持回放完整的 Agent 会话历史
- **验收标准**：
  - [ ] 实现消息历史存储
  - [ ] 支持暂停/继续
  - [ ] 支持进度拖拽
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-105 性能统计面板
- **目标**：统计 Token 消耗、工具调用耗时
- **验收标准**：
  - [ ] 创建 `PerformancePanel` 组件
  - [ ] 展示 Token 使用量
  - [ ] 展示工具调用次数和总耗时
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-106 Agent Console 页面
- **目标**：整合所有调试台组件
- **验收标准**：
  - [ ] 创建 `/agent-console` 页面
  - [ ] 布局：左侧消息历史 + 右侧工具调用/性能
  - [ ] 支持连接/断开/清空操作
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

### Phase 2：行动预览审批台（Q4）

#### H-201 行动计划生成器
- **目标**：基于用户请求生成完整的执行计划
- **验收标准**：
  - [ ] 创建 `ActionPlan` 模型
  - [ ] 实现计划生成逻辑
  - [ ] 支持步骤排序和依赖关系
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-202 风险评估器
- **目标**：自动评估每个步骤的风险等级
- **验收标准**：
  - [ ] 实现 `RiskAssessor` 服务
  - [ ] 支持 LOW/MEDIUM/HIGH/CRITICAL 四级评估
  - [ ] 基于工具类型和操作内容评估
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-203 预览审批流
- **目标**：高风险步骤自动触发审批流程
- **验收标准**：
  - [ ] 集成现有 Approval Guard
  - [ ] 支持 `SEND_EXTERNAL/MERGE_MAIN/DEPLOY_PROD` 等审批类型
  - [ ] 审批通过后自动执行
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-204 模拟执行（dry-run）
- **目标**：验证计划可行性，不实际执行
- **验收标准**：
  - [ ] 实现 dry-run 模式
  - [ ] 模拟工具调用结果
  - [ ] 展示模拟执行路径
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-205 计划修改与对比
- **目标**：允许人工调整执行顺序、参数
- **验收标准**：
  - [ ] 支持拖拽调整步骤顺序
  - [ ] 支持跳过某些步骤
  - [ ] 支持参数修改
  - [ ] 展示修改前后的差异
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-206 Action Plan 页面
- **目标**：整合所有预览审批组件
- **验收标准**：
  - [ ] 创建 `/action-plan` 页面
  - [ ] 展示完整执行计划
  - [ ] 支持生成/审批/执行/取消操作
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

### Phase 3：流水线编排器（Q4）

#### H-301 流水线定义 DSL
- **目标**：通过 YAML/JSON 定义流水线结构
- **验收标准**：
  - [ ] 定义流水线 Schema（stages/variables/errorPolicy）
  - [ ] 支持顺序/并行/条件执行模式
  - [ ] 支持超时和重试配置
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-302 可视化流水线编辑器
- **目标**：拖拽编排流水线
- **验收标准**：
  - [ ] 创建 `PipelineEditor` 组件
  - [ ] 支持拖拽添加阶段
  - [ ] 支持连接线配置
  - [ ] 支持 YAML/JSON 源码编辑
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-303 流水线执行引擎
- **目标**：按定义顺序执行各阶段
- **验收标准**：
  - [ ] 实现 `PipelineExecutor` 服务
  - [ ] 支持阶段间数据传递
  - [ ] 支持并行阶段并发执行
  - [ ] 支持条件分支判断
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-304 错误处理与回滚
- **目标**：阶段失败时的重试、降级、跳过策略
- **验收标准**：
  - [ ] 实现错误处理策略（stop/continue/rollback）
  - [ ] 支持自动重试
  - [ ] 支持手动回滚
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-305 流水线市场模板
- **目标**：预设常用流水线模板
- **验收标准**：
  - [ ] 实现代码发布流水线模板
  - [ ] 实现代码审查流水线模板
  - [ ] 实现故障响应流水线模板
  - [ ] 支持模板导入/导出
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-306 Pipeline 页面
- **目标**：整合所有流水线组件
- **验收标准**：
  - [ ] 创建 `/pipelines` 页面（列表）
  - [ ] 创建 `/pipeline/:id/edit` 页面（编辑器）
  - [ ] 创建 `/pipeline/:id/executions` 页面（执行历史）
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

### Phase 4：多 Agent 协作调度（Q1）

#### H-401 自然语言任务解析
- **目标**：解析用户的自然语言任务描述
- **验收标准**：
  - [ ] 实现 `TaskParser` 服务
  - [ ] 提取主目标、约束、预期输出
  - [ ] 识别优先级
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-402 任务自动拆解
- **目标**：自动将任务拆解为子任务
- **验收标准**：
  - [ ] 实现 `TaskDecomposer` 服务
  - [ ] 拆解为可执行的子任务
  - [ ] 分析任务依赖关系
  - [ ] 识别每个子任务需要的角色
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-403 Agent 智能匹配
- **目标**：根据子任务类型匹配最合适的 Agent
- **验收标准**：
  - [ ] 实现 `AgentMatcher` 服务
  - [ ] 支持角色匹配
  - [ ] 支持负载均衡
  - [ ] 支持能力匹配（检查 tools 授权）
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-404 协作上下文管理
- **目标**：维护跨 Agent 的共享上下文
- **验收标准**：
  - [ ] 实现 `CollaborationContext` 服务
  - [ ] 支持共享内存（Map）
  - [ ] 支持 Agent 间消息传递
  - [ ] 支持共享交付物
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-405 冲突解决策略
- **目标**：多个 Agent 输出冲突时的处理策略
- **验收标准**：
  - [ ] 实现冲突检测机制
  - [ ] 支持多种解决策略（latest/winner/merge/manual）
  - [ ] 支持人工介入
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-406 结果聚合与输出
- **目标**：汇总多个 Agent 的输出为最终结果
- **验收标准**：
  - [ ] 实现结果聚合逻辑
  - [ ] 支持多种聚合模式（sequential/merge/tree）
  - [ ] 生成最终报告
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

#### H-407 Orchestrator 页面
- **目标**：整合所有协作调度组件
- **验收标准**：
  - [ ] 创建 `/orchestrator` 页面
  - [ ] 支持自然语言任务输入
  - [ ] 展示任务拆解预览
  - [ ] 展示执行状态和结果
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

## 三、开源发布准备（Q4）

### O-001 清理内部代码/注释
- **目标**：移除内部信息，准备开源
- **验收标准**：
  - [ ] 检查所有代码文件，无内部信息泄露
  - [ ] 检查所有注释，无敏感信息
  - [ ] 检查文档，无内部链接
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### O-002 创建安装脚本
- **目标**：提供多平台安装方式
- **验收标准**：
  - [ ] npm 安装脚本
  - [ ] Homebrew 安装脚本（macOS）
  - [ ] Winget 安装脚本（Windows）
  - [ ] Linux 安装脚本（AppImage）
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### O-003 API 文档
- **目标**：完整的 API 参考文档
- **验收标准**：
  - [ ] OpenAPI/Swagger 规范
  - [ ] 所有端点文档
  - [ ] 请求/响应示例
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### O-004 演示视频
- **目标**：5 分钟 Demo 视频
- **验收标准**：
  - [ ] 核心功能演示
  - [ ] Agent Harness 演示
  - [ ] 上传 YouTube/Bilibili
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### O-005 CI/CD 流水线
- **目标**：GitHub Actions 自动构建和测试
- **验收标准**：
  - [ ] 自动化测试（unit/integration/e2e）
  - [ ] 自动化构建（Windows/macOS/Linux）
  - [ ] 自动化发布（GitHub Release）
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

## 四、安全加固（持续）

### S-001 第三方安全审计
- **目标**：专业安全团队审计
- **验收标准**：
  - [ ] 委托第三方安全公司
  - [ ] 完成安全审计报告
  - [ ] 修复发现的问题
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### S-002 渗透测试
- **目标**：模拟攻击测试系统安全性
- **验收标准**：
  - [ ] 完整的渗透测试报告
  - [ ] 修复发现的漏洞
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### S-003 Dependabot 依赖扫描
- **目标**：自动更新依赖，修复安全漏洞
- **验收标准**：
  - [ ] GitHub Dependabot 配置
  - [ ] 自动接收安全更新
  - [ ] 定期检查更新
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### S-004 代码签名
- **目标**：确保发布包来源可信
- **验收标准**：
  - [ ] Windows 代码签名
  - [ ] macOS 代码签名
  - [ ] Linux 包签名
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

## 五、待完成功能

### F-001 E2E 测试覆盖
- **目标**：16 个测试套件稳定运行
- **验收标准**：
  - [ ] 所有测试套件通过
  - [ ] 覆盖所有核心页面
  - [ ] 覆盖关键用户流程
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### F-002 多语言支持
- **目标**：支持中英文切换
- **验收标准**：
  - [ ] i18next 集成
  - [ ] 中文语言包
  - [ ] 英文语言包
  - [ ] 语言切换 UI
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### F-003 主题切换
- **目标**：支持浅色/深色/跟随系统
- **验收标准**：
  - [ ] 三种主题模式
  - [ ] 主题持久化
  - [ ] 平滑切换动画
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### F-004 插件系统
- **目标**：支持第三方扩展
- **验收标准**：
  - [ ] 插件 API 设计
  - [ ] 插件沙箱安全
  - [ ] 插件市场后端
  - [ ] 至少 3 个官方插件
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### F-005 数据导入/导出
- **目标**：支持数据迁移
- **验收标准**：
  - [ ] Workspace 导出（JSON）
  - [ ] Workspace 导入
  - [ ] 支持密钥重新输入
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

## 六、商业化准备（Q1）

### B-001 Pro 套餐设计
- **目标**：定义付费功能
- **验收标准**：
  - [ ] Pro 套餐功能列表
  - [ ] 定价策略（$29/月）
  - [ ] 免费版限制
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### B-002 支付集成
- **目标**：Stripe 支付
- **验收标准**：
  - [ ] Stripe 集成
  - [ ] 订阅管理
  - [ ] 账单系统
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### B-003 许可证管理
- **目标**：许可证密钥系统
- **验收标准**：
  - [ ] 许可证生成
  - [ ] 许可证验证
  - [ ] 许可证撤销
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### B-004 使用分析
- **目标**：收集使用数据
- **验收标准**：
  - [ ] 匿名使用统计
  - [ ] 仪表盘展示
  - [ ] 用户隐私保护
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

## 七、企业特性（Q2+）

### E-001 多租户架构
- **目标**：完全隔离的租户环境
- **验收标准**：
  - [ ] 租户隔离
  - [ ] 租户管理
  - [ ] 租户配额
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### E-002 SSO 集成
- **目标**：支持企业 SSO
- **验收标准**：
  - [ ] SAML 支持
  - [ ] OIDC 支持
  - [ ] SSO 配置 UI
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### E-003 RBAC
- **目标**：细粒度角色权限控制
- **验收标准**：
  - [ ] 角色定义
  - [ ] 权限分配
  - [ ] 权限验证中间件
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### E-004 审计报告导出
- **目标**：SIEM 集成
- **验收标准**：
  - [ ] 导出格式（JSON/XML/CEF）
  - [ ] 定时导出
  - [ ] SIEM 集成
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

### E-005 合规报告
- **目标**：SOC 2, GDPR 报告
- **验收标准**：
  - [ ] SOC 2 报告模板
  - [ ] GDPR 报告模板
  - [ ] 自动生成
- **状态**：`[ ]`
- **完成日期**：
- **验证结果**：

---

## 八、优先级总览

### P0 优先级（立即开始）

| 任务 | 类别 | 工作量 |
|------|------|--------|
| T-001 拆分 api-server.ts | 技术债 | 中 |
| T-008 服务层依赖注入 | 技术债 | 大 |

### P1 优先级（Q3 完成）

| 任务 | 类别 | 工作量 |
|------|------|--------|
| T-009 单元测试 | 技术债 | 大 |
| T-010 集成测试 | 技术债 | 中 |
| H-101~H-106 实时调试台 | Agent Harness | 中 |
| F-001 E2E 测试覆盖 | 待完成功能 | 中 |
| S-001~S-004 安全加固 | 安全 | 大 |

### P2 优先级（Q4 完成）

| 任务 | 类别 | 工作量 |
|------|------|--------|
| T-011 连接池管理 | 技术债 | 中 |
| T-012 性能监控 | 技术债 | 中 |
| H-201~H-206 行动预览 | Agent Harness | 大 |
| H-301~H-306 流水线编排 | Agent Harness | 大 |
| O-001~O-005 开源发布 | 开源 | 中 |

### P3 优先级（Q1 完成）

| 任务 | 类别 | 工作量 |
|------|------|--------|
| H-401~H-407 多Agent调度 | Agent Harness | 大 |
| F-002~F-005 剩余功能 | 待完成功能 | 中 |
| B-001~B-004 商业化 | 商业化 | 中 |

### P4 优先级（Q2+ 完成）

| 任务 | 类别 | 工作量 |
|------|------|--------|
| E-001~E-005 企业特性 | 企业 | 大 |

---

## 九、执行建议

### 第一步：T-001 路由拆分

从 `api-server.ts` 路由拆分开始，这是其他所有任务的基础：

```
1. 梳理当前 api-server.ts 中的所有路由
2. 按域分类：
   - tickets
   - approvals
   - team (roles/agents/tools)
   - comms (profiles/targets/messages)
   - jobs
   - models
   - backup
   - search
   - doctor
   - alerts
   - notification-policies
   - doctor-scheduler
   - operations
   - infrastructure
   - release
   - workspaces
   - workspace-settings
   - policies
   - deployments
   - openclaw
3. 每个域创建独立路由文件
4. 更新 api-server.ts 只保留 startServer()
```

### 第二步：T-008 依赖注入

```
1. 定义 ServiceDeps 接口
2. 重构 ApprovalGuard
3. 重构 ConfigManager
4. 重构 DoctorService
5. 保持向后兼容
```

### 第三步：T-009 单元测试

```
1. 搭建测试框架（Vitest/Jest）
2. 为 ApprovalGuard 编写测试
3. 为 audit-log-writer 编写测试
4. 为 api-shared 工具编写测试
5. 覆盖率 > 70%
```

---

*文档版本：v1.0 | 最后更新：2026-08-03*
